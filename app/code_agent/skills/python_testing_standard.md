# 测试开发基础规范

1. **测试框架选择**：本项目强制要求所有 Python 脚本的单测使用 `pytest` 框架。不得使用 `unittest`。
2. **测试位置**：测试代码不需要建立与主代码同名的独立目录，而是强制放置在专门的 tests 目录下，即 `tests/test_<原文件>.py`
3. **隔离性**：执行危险逻辑时优先确保 Mocking 或在 Docker 中运行，不要在测试中修改外部物理数据。
4. **命名规范**：测试函数必须以 `test_` 开始并说明其所测的功能或断境。